function clone-docs() {
	if [[ "$#" != 1 ]] ; then
		echo "Gimme a repo path, <type>/<name>"
		exit 1
	fi
	repopath="$1"

	cd ~/git/docs/
	if [ -d "$repopath" ] ; then
		cd "$repopath" && git pull
	else
		git clone "gollum@wiki1.syd1:~/${repopath}.git" "$repopath" && cd "$repopath"
	fi
}
